#include<stdio.h>
#include<stdlib.h>
#include<string.h>
#include<ctype.h>


int nameToValidate(char*); //function to validate name of user
int panToValidate(char*); //function to validate pan_id of user
int emailToValidate(char*); //function to validate email_id
int phoneNoToValidate(long int); //function to validate phone number
int usernameToValidate(char*); //function to validate username
int passwordToValidate(char *);//function to validate password
int idToValidate(int); //function to validate id
int dateToValidate(int,int,int);//funtion to validate date in the form of dd/mm/yyyy
